# Redirect npm cache away from node_modules to avoid Docker mount conflicts
# (Railway nixpacks mounts /app/node_modules/.cache as a build cache layer;
# npm ci/install writing to the same path causes EBUSY on rmdir)
cache=/tmp/.npm-cache
