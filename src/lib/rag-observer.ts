import { query } from '../db/pool.js';
import { ingestDocument, searchDocuments, type IngestParams, type Document } from './rag.js';
export interface RagObservation { id:number; query_text:string|null; collection:string; results_count:number; top_similarity:number|null; latency_ms:number; model_used:string; created_at:string; }
const rec = (qt:string|null,col:string,cnt:number,top:number|null,ms:number,mod:string) =>
  query(`INSERT INTO meta.rag_observations (query_text,collection,results_count,top_similarity,latency_ms,model_used) VALUES ($1,$2,$3,$4,$5,$6)`,[qt,col,cnt,top,ms,mod]).catch(()=>{});
export async function observedIngest(doc:IngestParams): Promise<number> { const m=process.env.OPENAI_EMBEDDING_MODEL??'text-embedding-3-small'; const t=Date.now(); const id=await ingestDocument(doc); rec(null,doc.collection??'default',1,null,Date.now()-t,m); return id; }
export async function observedSearch(p:Parameters<typeof searchDocuments>[0]): Promise<Document[]> { const m=process.env.OPENAI_EMBEDDING_MODEL??'text-embedding-3-small'; const t=Date.now(); const r=await searchDocuments(p); const top=r.length>0&&r[0].similarity!=null?Number(r[0].similarity):null; rec(p.query,p.collection??'default',r.length,top,Date.now()-t,m); return r; }
export async function listObservations(p:{collection?:string;limit?:number}): Promise<RagObservation[]> { return query<RagObservation>(`SELECT id,query_text,collection,results_count,top_similarity,latency_ms,model_used,created_at FROM meta.rag_observations WHERE ($1::text IS NULL OR collection=$1) ORDER BY created_at DESC LIMIT $2`,[p.collection??null,Math.min(p.limit??50,200)]); }
